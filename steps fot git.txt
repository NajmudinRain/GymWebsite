1. $ cd "C:\Users\User\Documents\Web_Dev\GymWebsite" /*setting directory to the workong directory.

2. $ git config --global user.name "NajmudinRain"

3. git config --global user.email "najmudinrain786@gmail.com"

4. $ touch .gitignore  /* create the ignore file

5. git init   /*intialize the git

6.  git add .    /*put all the content in the staging area, where we are not worried.

7. git commit -m "Initail commit"

8. git status  /* just to see  

9. git remote add origin git@github.com:NajmudinRain/GymWebsite.git

10. git push origin master 
/*(The authenticity of host 'github.com (13.229.188.59)' can't be established)

11.ssh-keygen -t ed25519 -C "najmudinrain786@gmail.com" /*(generating ssh keys) press enter 2-3 times

12. cat /c/Users/User/.ssh/id_ed25519.pub  /*(the given path is public key path given after executing above command)
           /* ssh keys will be genrating which we will add in github ssh keys section)

13  git push origin master



